<h1><%= t('.sign_in') %></h1>

<div class="row justify-content-center">
  <div class="col-12 mt-4">
    <%= form_with(model: resource, as: resource_name, url: session_path(resource_name), class: "shadow p-3 mb-3 bg-success rounded", local: true) do |f| %>
      <div class="form-group row">
        <div class="col-2">
          <%= f.label :email, class: "col-form-label" %>
        </div>
        <div class="col-10">
          <%= f.email_field :email, autofocus: true, class: "form-control shadow rounded", placeholder: "Enter a email" %>
        </div>
      </div>

      <div class="form-group row">
        <div class="col-2">
          <%= f.label :password, class: "col-form-label" %>
        </div>
        <div class="col-10">
          <%= f.password_field :password, class: "form-control shadow rounded", placeholder: "Enter a password" %>
        </div>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="form-group form-check">
          <%= f.check_box :remember_me, class: 'form-check-input' %>
          <%= f.label :remember_me, class: 'form-check-label' do %>
            <%= resource.class.human_attribute_name('remember_me') %>
          <% end %>
        </div>
      <% end %>

      <div class="form-group row justify-content-center">
        <%= f.submit t('.sign_in'), class: "btn btn-outline-light btn-lg" %>
      </div>
    <% end %>

    <%= render 'devise/shared/links' %>
  </div>
</div>
