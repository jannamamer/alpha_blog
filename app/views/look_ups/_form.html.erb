<%= render 'layouts/errors', obj: @look_up %>
<div class="row justify-content-center">
  <div class="col-12 mt-4">
    <%= form_with(model: @look_up, class: "shadow p-3 mb-3 bg-success rounded", local: true) do |f| %>
      <div class="form-group row">
        <div class="col-2">
          <%= f.label :group, "Look up group", class: "col-form-label" %>
        </div>
        <div class="col-10">
          <%= f.text_field :group, autofocus: true, class: "form-control shadow rounded", placeholder: "Enter a look up group" %>
        </div>
      </div>
      <div class="form-group row">
        <div class="col-2">
          <%= f.label :name, "Look up name", class: "col-form-label" %>
        </div>
        <div class="col-10">
          <%= f.text_field :name, class: "form-control shadow rounded", placeholder: "Enter a look up name" %>
        </div>
      </div>
      <div class="form-group row justify-content-center">
        <%= f.submit class: "btn btn-outline-light btn-lg" %>
      </div>
    <% end %>
  </div>
</div>
